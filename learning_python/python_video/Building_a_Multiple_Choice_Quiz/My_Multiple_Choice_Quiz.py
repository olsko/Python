from random import choices


questions = ["How many months are there?", ]
choices = []
def quiz(questions, choices):
    for question in len(range(questions)):
        print_question(questions[question], )

def print_question(q, ch):
    print(q)

quiz(questions, 1)